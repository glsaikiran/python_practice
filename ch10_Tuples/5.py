print((0,1,2) < (5,1,2))  #checks from left to right and compares if any returns true it gives output true if all are false it returns false
print(('jone','sally') < ('jone','sam')) # first j compared to j,o to o,etc until 'jone' and 'jone' completes next goes s to s, go on here anly l<m gives true,so output gives true
